query GetFollowedEvents {
  followedEvents {
    id
    title
    description
    picture
    location
    category
    date
    createdBy {
      id
      name
    }
  }
}

query GetMyEvents($fetchAll: Boolean, $page: Int) {
  myEvents(fetchAll: $fetchAll, page: $page) {
    id
    title
    description
    picture
    location
    category
    date
  }
}

query GetEvent($id: ID!) {
  event(id: $id) {
    id
    title
    description
    picture
    location
    category
    date
    price
    priceCurrency
    createdBy {
      id
      name
    }
  }
}

mutation CreateEvent($input: CreateEventInput!) {
  createEvent(input: $input)
}

mutation UpdateEvent($input: CreateEventInput!) {
  updateEvent(input: $input)
}

mutation DeleteEvent($id: ID!) {
  deleteEvent(id: $id)
}
